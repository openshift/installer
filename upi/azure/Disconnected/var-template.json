{
    "builders": [
      {
        "type": "azure-arm",
        "name": "azure-arm-vhd",
        "tenant_id": "{{user `azure_tenant_id`}}",
        "subscription_id": "{{user `azure_subscription_id`}}",
        "client_id": "{{user `azure_client_id`}}",
        "client_secret": "{{user `azure_client_secret`}}",        
        "capture_container_name": "packerbuild",
        "capture_name_prefix": "{{user `image_capture_name`}}",
        "resource_group_name": "{{user `azure_final_rg_name`}}",
        "storage_account": "{{user `azure_final_sa_name`}}",
        "location": "East US 2",
        "os_type": "Linux",
        "image_publisher": "RedHat",
        "image_offer": "RHEL",
        "image_sku": "7-RAW",
        "azure_tags": {
          "image": "yum repo"
        },
        "vm_size": "Standard_DS3_v2",
        "os_disk_size_gb": 32,
        "disk_additional_size": [128]
      }
    ],
    "provisioners": [
        
    ]
}