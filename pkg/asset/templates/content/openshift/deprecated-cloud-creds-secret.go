package openshift

import (
	"os"
	"path/filepath"

	"github.com/openshift/installer/pkg/asset"
	"github.com/openshift/installer/pkg/asset/templates/content"
)

const (
	deprecatedCloudCredsSecretFileName = "deprecated-cloud-creds-secret.yaml.template"
)

var _ asset.WritableAsset = (*DeprecatedCloudCredsSecret)(nil)

// DeprecatedCloudCredsSecret is the constant to represent contents of corresponding yaml file
type DeprecatedCloudCredsSecret struct {
	FileList []*asset.File
}

// Dependencies returns all of the dependencies directly needed by the asset
func (t *DeprecatedCloudCredsSecret) Dependencies() []asset.Asset {
	return []asset.Asset{}
}

// Name returns the human-friendly name of the asset.
func (t *DeprecatedCloudCredsSecret) Name() string {
	return "DeprecatedCloudCredsSecret"
}

// Generate generates the actual files by this asset
func (t *DeprecatedCloudCredsSecret) Generate(parents asset.Parents) error {
	fileName := deprecatedCloudCredsSecretFileName
	data, err := content.GetOpenshiftTemplate(fileName)
	if err != nil {
		return err
	}
	t.FileList = []*asset.File{
		{
			Filename: filepath.Join(content.TemplateDir, fileName),
			Data:     []byte(data),
		},
	}
	return nil
}

// Files returns the files generated by the asset.
func (t *DeprecatedCloudCredsSecret) Files() []*asset.File {
	return t.FileList
}

// Load returns the asset from disk.
func (t *DeprecatedCloudCredsSecret) Load(f asset.FileFetcher) (bool, error) {
	file, err := f.FetchByName(filepath.Join(content.TemplateDir, deprecatedCloudCredsSecretFileName))
	if err != nil {
		if os.IsNotExist(err) {
			return false, nil
		}
		return false, err
	}
	t.FileList = []*asset.File{file}
	return true, nil
}
