apiVersion: v1
kind: Pod
metadata:
  name: bootstrap-kube-scheduler
  namespace: kube-system
spec:
  hostNetwork: true
  containers:
  - name: kube-scheduler
    image: ${hyperkube_image}
    command:
    - ./hyperkube
    - scheduler
    - --kubeconfig=/etc/kubernets/kubeconfig
    - --leader-elect=true
    volumeMounts:
    - name: kubernetes
      mountPath: /etc/kubernetes
      readOnly: true
  volumes:
  - name: kubernetes
    hostPath:
      path: /etc/kubernetes
