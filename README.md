# Openshift Installer terraform providers

## Supported providers


## Quick Start

Clone this repository. Then build the terraform providers with

```sh
make
```

This will create `bin/terraform-provider-$name` and `bin/terraform-provider-$name.zip`.
